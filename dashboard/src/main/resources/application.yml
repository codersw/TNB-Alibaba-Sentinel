server:
  port: 8080
spring:
  application:
    name: dashboard
  http:
    encoding:
      force: true
      charset: UTF-8
      enabled: true
  profiles:
    active: prod
sentinel:
  dashboard:
    version: ${project.version}
